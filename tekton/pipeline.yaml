apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: pipeline
  namespace: oauth-demo-app
spec:
  # params:
  #   - name: radix
  #     type: object
  #     properties:
  #       git-ssh-url:
  #         type: string
  #       git-commit:
  #         type: string
  # params:
  #   - name: sshurl
  #     type: string
  #     description: SSH URL
  #   - name: commit
  #     type: string
  #     description: Commit
  tasks:
    - name: some-build-task
      taskRef:
        name: chat
      # params:
      #   - name: radix
      #     value:
      #       git-ssh-url: "$(params.radix.git-ssh-url)"
      #       git-commit: "$(params.radix.git-commit)"
      # params:
      #   - name: radix
      #     value:
      #       gitSSHUrl: "$(params.sshurl)"
      #       gitCommit: "$(params.commit)"
