apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: db
spec:
  # volumes:
  #   - name: host-fs
  #     hostPath:
  #       path: /
  steps:
  - image: alpine
    name: init-resources
    script: |
      #!/usr/bin/env sh
      echo "init resources"
      :
  - image: alpine
    name: migrate-db
    script: |
      #!/usr/bin/env sh
      echo "migrate database"
      :

