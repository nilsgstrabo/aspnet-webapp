@page
@model PrivacyModel
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

<div>Claims</div>

@foreach (var item in Model.User.Claims)
{
    <div>@item.ValueType : @item.Value</div>
}